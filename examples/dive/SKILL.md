---
name: dive
description: **A tool for exploring a Docker image, layer contents, and discovering ways to shrink the size of your Docker/OCI image.** WHEN: run `dive` commands. Triggers: use dive, install dive, how to use dive,
source_repo: https://github.com/wagoodman/dive
generated_at: "2026-03-15T23:18:11.713Z"
---

# dive

**A tool for exploring a Docker image, layer contents, and discovering ways to shrink the size of your Docker/OCI image.**

## When to Use

- Run `dive` commands

## When NOT to Use

- GUI or web-based workflows where CLI is not available
- Projects using Python or JavaScript (different ecosystem)

## Quick Start

### Install

```bash
DIVE_VERSION=$(curl -sL "https://api.github.com/repos/wagoodman/dive/releases/latest" | grep '"tag_name":' | sed -E 's/.*"v([^"]+)".*/\1/')
curl -fOL "https://github.com/wagoodman/dive/releases/download/v${DIVE_VERSION}/dive_${DIVE_VERSION}_linux_amd64.deb"
sudo apt install ./dive_${DIVE_VERSION}_linux_amd64.deb
```
```bash
sudo snap install docker
sudo snap install dive
sudo snap connect dive:docker-executables docker:docker-executables
sudo snap connect dive:docker-daemon docker:docker-daemon
```

## CLI Commands

- `dive`

## Docker

- **Base image:** `alpine:3.21`
- **Entrypoint:** `["/usr/local/bin/dive"]`

## Project Info

- **Language:** C, Go
- **Tests:** Yes
- **Key dependencies:** github.com/anchore/clio, github.com/anchore/go-logger, github.com/awesome-gocui/gocui, github.com/awesome-gocui/keybinding, github.com/cespare/xxhash/v2, github.com/charmbracelet/lipgloss, github.com/docker/cli, github.com/docker/docker

## File Structure

```
├── cmd/
│   └── dive/
├── dive/
│   ├── filetree/
│   ├── image/
│   └── get_image_resolver.go
├── internal/
│   ├── bus/
│   ├── log/
│   └── utils/
├── Dockerfile
├── go.mod
├── go.sum
├── LICENSE
├── Makefile
├── README.md
├── RELEASE.md
└── Taskfile.yaml
```

> Generated by [repo2skill](https://github.com/NeuZhou/repo2skill)