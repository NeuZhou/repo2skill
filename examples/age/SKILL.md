---
name: age
description: age is a simple, modern and secure file encryption tool, format, and Go library. WHEN: run `age` commands, lint or format code. Triggers: use age, install age, how to use age, run age.
source_repo: https://github.com/FiloSottile/age
generated_at: "2026-03-15T23:18:11.027Z"
---

# age

age is a simple, modern and secure file encryption tool, format, and Go library.

## When to Use

- Run `age` commands
- Lint or format code

## When NOT to Use

- GUI or web-based workflows where CLI is not available
- Projects using Python or JavaScript (different ecosystem)

## Quick Start

### Install

```
https://dl.filippo.io/age/latest?for=linux/amd64
https://dl.filippo.io/age/v1.3.1?for=darwin/arm64
...
```
```
go install filippo.io/age/cmd/...@latest
```

### Basic Usage

```
Usage:
    age [--encrypt] (-r RECIPIENT | -R PATH)... [--armor] [-o OUTPUT] [INPUT]
    age [--encrypt] --passphrase [--armor] [-o OUTPUT] [INPUT]
    age --decrypt [-i PATH]... [-o OUTPUT] [INPUT]

Options:
    -e, --encrypt               Encrypt the input to the output. Default if omitted.
    -d, --decrypt               Decrypt the input to the output.
    -o, --output OUTPUT         Write the result to the file at path OUTPUT.
    -a, --armor                 Encrypt to a PEM encoded format.
    -p, --passphrase            Encrypt with a passphrase.
    -r, --recipient RECIPIENT   Encrypt to the specified RECIPIENT. Can be repeated.
    -R, --recipients-file PATH  Encrypt to recipients listed at PATH. Can be repeated.
    -i, --identity PATH         Use the identity file at PATH. Can be repeated.

INPUT defaults to standard input, and OUTPUT defaults to standard output.
If OUTPUT exists, it will be overwritten.

RECIPIENT can be an age public key generated by age-keygen ("age1...")
or an SSH public key ("ssh-ed25519 AAAA...", "ssh-rsa AAAA...").

Recipient files contain one or more recipients, one per line. Empty lines
and lines starting with "#" are ignored as comments. "-" may be used to
read recipients from standard input.

Identity files contain one or more secret keys ("AGE-SECRET-KEY-1..."),
one per line, or an SSH key. Empty lines and lines starting with "#" are
ignored as comments. Passphrase encrypted age files can be used as
identity files. Multiple key files can be provided, and any unused ones
will be ignored. "-" may be used to read identities from standard input.

When --encrypt is specified explicitly, -i can also be used to encrypt to an
identity file symmetrically, instead or in addition to normal recipients.
```

## CLI Commands

- `age`

## Project Info

- **Language:** Go
- **Tests:** Yes
- **Key dependencies:** filippo.io/edwards25519, filippo.io/hpke, filippo.io/nistec, golang.org/x/crypto, golang.org/x/sys, golang.org/x/term

## File Structure

```
├── agessh/
│   ├── agessh_test.go
│   ├── agessh.go
│   └── encrypted_keys.go
├── armor/
│   ├── armor_test.go
│   └── armor.go
├── cmd/
│   ├── age/
│   ├── age-inspect/
│   ├── age-keygen/
│   └── age-plugin-batchpass/
├── doc/
│   ├── age-inspect.1
│   ├── age-inspect.1.html
│   ├── age-inspect.1.ronn
│   ├── age-keygen.1
│   ├── age-keygen.1.html
│   ├── age-keygen.1.ronn
│   ├── age-plugin-batchpass.1
```

> Generated by [repo2skill](https://github.com/NeuZhou/repo2skill)