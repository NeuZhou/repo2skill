---
name: prettier
description: Prettier is an opinionated code formatter. It enforces a consistent style by parsing your code and re-printing it with its own rules that take the maximum line length into account, wrapping code when 
source_repo: https://github.com/prettier/prettier
generated_at: "2026-03-15T23:18:54.645Z"
---

# prettier

Prettier is an opinionated code formatter. It enforces a consistent style by parsing your code and re-printing it with its own rules that take the maximum line length into account, wrapping code when necessary.

## When to Use

- Run `prettier` commands
- Make HTTP requests
- Lint or format code
- Parse data or files

## When NOT to Use

- GUI or web-based workflows where CLI is not available
- Projects using Python or Rust (different ecosystem)

## Quick Start

### Install

```bash
npm install prettier
```

## CLI Commands

- `prettier`

## Packages

This is a monorepo containing the following packages:

- `plugin-hermes`
- `plugin-oxc`

## Key API

- `__debug`
- `__internal`
- `check`
- `clearConfigCache`
- `format`
- `formatWithCursor`
- `getSupportInfo`
- `resolveConfig`
- `resolveConfigFile`
- `getFileInfo`

## Project Info

- **Language:** JavaScript, TypeScript
- **License:** MIT
- **Tests:** Yes
- **Key dependencies:** @angular/compiler, @babel/code-frame, @babel/parser, @babel/types, @glimmer/syntax, @prettier/cli, @prettier/html-event-attributes, @prettier/html-tags

## File Structure

```
├── benchmarks/
│   ├── get-preferred-quote.js
│   ├── print-doc-to-string.js
│   ├── string-buffer-compress.js
│   ├── string-concatenation.js
│   ├── trim-indentation.js
│   └── utilities.js
├── bin/
│   ├── prettier.cjs
│   └── prettier.js
├── changelog_unreleased/
│   ├── angular/
│   ├── api/
│   ├── cli/
│   ├── css/
│   ├── flow/
│   ├── graphql/
│   ├── handlebars/
│   ├── html/
│   ├── javascript/
```

> Generated by [repo2skill](https://github.com/NeuZhou/repo2skill)