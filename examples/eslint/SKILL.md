---
name: eslint
description: The people who review and implement new features. WHEN: run `eslint` commands. Triggers: use eslint, install eslint, how to use eslint, run eslint.
source_repo: https://github.com/eslint/eslint
generated_at: "2026-03-15T23:18:45.313Z"
---

# eslint

The people who review and implement new features.

## When to Use

- Run `eslint` commands

## When NOT to Use

- GUI or web-based workflows where CLI is not available
- Projects using Python or Rust (different ecosystem)

## Quick Start

### Install

```bash
npm install eslint
```

### Basic Usage

```js
import { defineConfig } from "eslint/config";

export default defineConfig([
	{
		files: ["**/*.js", "**/*.cjs", "**/*.mjs"],
		rules: {
			"prefer-const": "warn",
			"no-constant-binary-expression": "error",
		},
	},
]);
```

## CLI Commands

- `eslint`

## Configuration

```js
import { defineConfig } from "eslint/config";

export default defineConfig([
	{
		files: ["**/*.js", "**/*.cjs", "**/*.mjs"],
		rules: {
			"prefer-const": "warn",
			"no-constant-binary-expression": "error",
		},
	},
]);
```

## Packages

This is a monorepo containing the following packages:

- `eslint-config-eslint`
- `js`

## Project Info

- **Language:** JavaScript, TypeScript
- **License:** MIT
- **Tests:** Yes
- **Key dependencies:** @eslint-community/eslint-utils, @eslint-community/regexpp, @eslint/config-array, @eslint/config-helpers, @eslint/core, @eslint/plugin-kit, @humanfs/node, @humanwhocodes/module-importer

## File Structure

```
├── bin/
│   └── eslint.js
├── conf/
│   ├── ecma-version.js
│   ├── globals.js
│   ├── replacements.json
│   └── rule-type-list.json
├── docs/
│   ├── _examples/
│   ├── src/
│   ├── tools/
│   ├── netlify.toml
│   ├── package.json
│   ├── postcss.config.js
│   └── README.md
├── lib/
│   ├── cli-engine/
│   ├── config/
│   ├── eslint/
│   ├── languages/
```

> Generated by [repo2skill](https://github.com/NeuZhou/repo2skill)