---
name: axios
description: Buy real Instagram followers from Twicsy starting at only $2.97. Twicsy has been voted the best site... twicsy.com Hi, we&#x27;re Descope! We are building something in the authentication space for app
source_repo: https://github.com/axios/axios
generated_at: "2026-03-15T23:09:17.346Z"
---

# axios

Buy real Instagram followers from Twicsy starting at only $2.97. Twicsy has been voted the best site... twicsy.com Hi, we&#x27;re Descope! We are building something in the authentication space for app developers and... Website | Docs | Community Best Route Planning And Route Optimization Software Explore | Free Trial | Contact At Buzzoid, you can buy Instagram followers quickly, safely, and easily with just a few clicks. Rate... buzzoid.com Buy Instagram Likes poprey.com A lightweight open-source API Development, Testing Mocking platform requestly.com 💜 Become a sponsor 💜 Become a sponsor

## When to Use

- Make HTTP requests
- Interact with REST APIs
- Run or write tests
- Build command-line interfaces

## When NOT to Use

- Projects using Python or Rust (different ecosystem)

## Quick Start

### Install

```bash
npm install axios
```

### Basic Usage

```js
import axios from "axios";
//const axios = require('axios'); // legacy way

try {
  const response = await axios.get("/user?ID=12345");
  console.log(response);
} catch (error) {
  console.error(error);
}

// Optionally the request above could also be done as
axios
  .get("/user", {
    params: {
      ID: 12345,
    },
  })
  .then(function (response) {
    console.log(response);
  })
  .catch(function (error) {
    console.log(error);
  })
  .finally(function () {
    // always executed
  });

// Want to use async/await? Add the `async` keyword to your outer function/method.
async function getUser() {
  try {
    const response = await axios.get("/user?ID=12345");
    console.log(response);
  } catch (error) {
    console.error(error);
  }
}
```

```js
const response = await axios.post("/user", {
  firstName: "Fred",
  lastName: "Flintstone",
});
console.log(response);
```

```js
function getUserAccount() {
  return axios.get("/user/12345");
}

function getUserPermissions() {
  return axios.get("/user/12345/permissions");
}

Promise.all([getUserAccount(), getUserPermissions()]).then(function (results) {
  const acct = results[0];
  const perm = results[1];
});
```

## Key Features

- Browser Requests: Make XMLHttpRequests directly from the browser.
- Node.js Requests: Make http requests from Node.js environments.
- Promise-based: Fully supports the Promise API for easier asynchronous code.
- Interceptors: Intercept requests and responses to add custom logic or transform data.
- Data Transformation: Transform request and response data automatically.

## Examples

> **Note**: `async/await` is part of ECMAScript 2017 and is not supported in Internet
> Explorer and older browsers, so use with caution.

Performing a `POST` request



Performing multiple concurrent requests

## Key API

- `default`
- `Axios`
- `AxiosError`
- `CanceledError`
- `isCancel`
- `CancelToken`
- `VERSION`
- `all`
- `Cancel`
- `isAxiosError`

## Project Info

- **Language:** JavaScript, TypeScript
- **License:** MIT
- **Tests:** Yes
- **Key dependencies:** follow-redirects, form-data, proxy-from-env

## File Structure

```
├── bin/
│   ├── actions/
│   ├── helpers/
│   ├── api.js
│   ├── check-build-version.js
│   ├── contributors.js
│   ├── GithubAPI.js
│   ├── githubAxios.js
│   ├── injectContributorsList.js
│   ├── pr.js
│   ├── repo.js
│   ├── RepoBot.js
│   ├── resolveNPMTag.js
│   ├── run-karma-tests.js
│   ├── sponsors.js
│   └── ssl_hotfix.js
├── examples/
│   ├── abort-controller/
│   ├── all/
│   ├── amd/
```

> Generated by [repo2skill](https://github.com/NeuZhou/repo2skill)